server:
  port: 8081

spring:
  application:
    name: config-client-test
  cloud:
    config:
      token: myroot #for vault token to get data
      uri: http://localhost:8888

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

security.basic.enabled: false
management.security.enabled: false

info:
  app:
    name: SPRING BOOT CONFIG SERVER CLIENT
    description: spring boot configuration server client
    version: 1.0.0